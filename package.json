{
  "name": "msmeawards",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Presidential MSME Awards — static site with a secure Node.js contact API (Vercel) and a React + Axios contact form.",
  "scripts": {
    "build": "vite build",
    "dev:widget": "vite build --watch",
    "test:api": "node --test",
    "start": "vercel dev"
  },
  "dependencies": {
    "axios": "^1.7.7"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "vite": "^5.4.11"
  },
  "engines": {
    "node": ">=18"
  }
}
