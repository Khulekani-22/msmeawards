# Secrets — never commit real credentials (use Vercel env vars in production)
.env
.env.*
!.env.example

# Node / build
node_modules/
dist/

# Vercel
.vercel/

# OS / editor cruft
.DS_Store
Thumbs.db
